# FoxuLaserCuts V1.0 - Manual de Entrega

**Misión**: Convertir >27,000 archivos de diseño CNC en una aplicación de catálogo ultra-rápida, portable, estética y personalizada ("Foxu").

## 🏆 Resultado Final
Has recibido **`FoxuLaserCuts.exe`** (aprox 75 MB).
Este único archivo contiene:
- **4,134 Diseños** (Todos los JPG/PNG válidos encontrados).
- **Motor Web Moderno**: HTML5/CSS3 con diseño Cyberpunk Neón.
- **Icono Remasterizado**: Alta definición y transparencia.

---

## 🚀 Cómo Usar
1.  **Ejecutar**: Doble clic en `FoxuLaserCuts.exe`. No requiere instalación.
2.  **Buscar**: Usa la barra superior. Escribe "Lobo", "Box", "Tree"...
3.  **Filtrar**: Pulsa los botones de categoría (estilo gafas láser) para filtrar al instante.
4.  **Descargar**:
    - Haz clic en cualquier diseño.
    - Se abre el modal con la imagen gigante.
    - Pulsa el botón **FOXU FIRE** ("Descargar Paquete") para ir a la fuente.

## 🛠️ Cómo Actualizar el Catálogo (Futuro)
Si descargas más archivos y quieres meterlos en la app:

1.  Pon tus carpetas nuevas en la carpeta del proyecto.
2.  Abre PowerShell en esa carpeta.
3.  Ejecuta:
    ```powershell
    .\Ingest_Catalog.ps1
    ```
    *(Esto escaneará todo y creará las miniaturas optimizadas)*.
4.  Ejecuta:
    ```powershell
    .\Build_EXE.ps1
    ```
    *(Esto creará un nuevo `FoxuLaserCuts.exe` con el contenido actualizado)*.

## 🎨 Personalización Técnica
El diseño es 100% código (`css/style.css`).
- **Naranja**: Gradiente Foxu (`#ff9d00` a `#ff0055`).
- **Cian**: Gradiente Láser (`#00f0ff` a `#0066ff`).
- **Layout**: Header Flexbox con alineación forzada a la derecha para "Online/Huggingface".

## 🚀 Rendimiento Extremo (V6 + V7)
Hemos blindado la aplicación para soportar **+100,000 diseños**:
- **Infinite Scroll (JS - V6)**: Carga inteligente de elementos (Lotes de 50). El navegador **nunca** se congela, aunque tengas 10TB de diseños.
- **PowerShell List<T> (V6)**: El script de ingesta ahora usa listas genéricas de .NET en lugar de arrays, permitiendo añadir 11,000 imágenes en segundos.
- **Smart Cleaning Cache (V11)**: El ejecutable usa la carpeta fija `AppData/FoxuLaserCuts_Data`.
    - **Inteligente**: Compara su **Build ID** interno con la versión en disco.
    - **Autolimpiable**: Si hay una actualización, borra la versión antigua y extrae la nueva automáticamente.
    - *Velocidad*: Inicios subsiguientes siguen siendo instantáneos.
- **Modo Eco Automático (V10)**: Detecta GPUs lentas en <0.5s y activa el modo ahorro sin intervención del usuario.

## 🧠 Lógica de Categorización (V5)
Hemos implementado un sistema de clasificación "Smart Regex" en `Ingest_Catalog.ps1`:
- **Function > Form**: Prioriza la función (Lámpara) sobre la forma (Caballo).
- **Regex Blindado**: Evita falsos positivos detectando palabras completas (ej: `cat` no activa con `education`).
- **Nuevas Categorías**: Incluye `ARQUITECTURA` (Torres, Edificios) y `ORGANIZADORES`.
- **Inteligencia Local (Opcional)**: Se incluye `classify_ai.py` para análisis de visión artificial con TensorFlow si se desea en el futuro.
Hemos implementado un sistema de clasificación "Smart Regex" en `Ingest_Catalog.ps1`:
- **Function > Form**: Prioriza la función (Lámpara) sobre la forma (Caballo).
- **Regex Blindado**: Evita falsos positivos detectando palabras completas (ej: `cat` no activa con `education`).
- **Nuevas Categorías**: Incluye `ARQUITECTURA` (Torres, Edificios) y `ORGANIZADORES`.
- **Inteligencia Local (Opcional)**: Se incluye `classify_ai.py` para análisis de visión artificial con TensorFlow si se desea en el futuro.

---
*Generated by Antigravity Omega for Foxu*
